-- ═══════════════════════════════════════════════════════════
-- Міграція: core.deadlines add naive_due_on
-- ═══════════════════════════════════════════════════════════

-- Дата до перенесення календарем (вихідний/свято → найближчий робочий день), але вже з
-- урахуванням зупинень строку. Разом з due_on дає змогу пояснити, чи й звідки саме стався
-- перенос (SEN-28), без повторного звернення до вже, можливо, зміненого стану core.holidays.
-- Для рядків, порахованих до SEN-28, значення невідоме - лишаємо NULL, заднім числом
-- не відновлюється.
ALTER TABLE core.deadlines
    ADD COLUMN naive_due_on DATE;
