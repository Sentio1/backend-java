services:
  user-postgres:
    image: postgres:17-alpine
    container_name: postgres_user_service_container

    ports:
      - "5432:5432"

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_USER_SERVICE_DB}

    restart: unless-stopped

    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U postgres -d ${POSTGRES_USER_SERVICE_DB}" ]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

    volumes:
      - user_postgres_data:/var/lib/postgresql/data/

  user-redis:
    image: redis:8-alpine
    container_name: redis_user_service_container
    ports:
      # Loopback-only: only this host's own processes (the app run via bootRun)
      # need to reach it - nothing outside the host should be able to.
      - "127.0.0.1:6379:6379"
    # No fallback default - an unset REDIS_PASSWORD must fail `docker compose up`
    # loudly instead of quietly starting Redis with a well-known password.
    command: redis-server --requirepass ${REDIS_PASSWORD:?REDIS_PASSWORD must be set}
    restart: unless-stopped
    volumes:
      - user_redis_data:/data

volumes:
  user_postgres_data:
  user_redis_data: